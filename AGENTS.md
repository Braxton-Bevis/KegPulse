# KegPulse contributor guidance

Preserve raw pulse evidence and historical records. Never acknowledge a device result before its durable database transaction commits. Keep firmware, host, and browser boundaries described in `docs/DECISIONS.md`. Run the relevant tests after every behavioral change; protocol changes require both Python and native firmware fixtures.

