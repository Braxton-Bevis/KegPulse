[Unit]
Description=KegPulse local flow monitor
After=default.target

[Service]
Type=simple
WorkingDirectory=@REPO_ROOT@
Environment=@DATA_ENV@
ExecStart=@PYTHON@ -m kegpulse --no-browser --port @PORT@
Restart=on-failure
RestartSec=3
TimeoutStopSec=15
UMask=0077

[Install]
WantedBy=default.target
